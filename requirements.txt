# Driver Monitoring System — Python dependencies
# Install with:  pip install -r requirements.txt

# Core computer vision
opencv-python>=4.9.0
mediapipe==0.10.14

# Numerical computing
numpy>=1.26.0

# Audio alerts (optional — alerts fall back gracefully if absent)
pygame-ce>=2.4.0

# Testing
pytest>=8.1.0

# Development / linting (optional)
# ruff>=0.4.0
# mypy>=1.9.0
